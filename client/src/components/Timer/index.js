import React, { useState } from 'react';
import { Link } from 'react-router-dom';


const TimerPanel = () => {
    const [currentTime, setTime] = useState(100);
    const [timerRunning, setTimerRunning] = useState(false);
}